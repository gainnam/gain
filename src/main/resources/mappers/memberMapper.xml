<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- memberMapper.xml: 회원관리에 대한 모든 쿼리를 모아서 관리하는 역할
세선 팩토리(Mybatis SessionFactory클래스)에서 쿼리를 생성합니다.
이 쿼리를 호출할 때 사용하는 이름이 namespace값이 됩니다. 
이름이 memberMapper안에 쿼리 여러 개 만들게 되는데, 구분해서 호출할 때, 사용하는 id-->
<mapper namespace="memberMapper">
<!-- 아래 반환값이 MemberVO클래스형이면, select * from 쿼리에서 결과값이 자동바인딩됩니다.
단,멤버 클래스의 멤버 변수 값이 tbl_member 테이블의 필드명과 일치하게 만들어야 하는 조건. -->
<select id="selectMember" resultType="org.edu.vo.MemberVO">
select * from tbl_member
</select>

<insert id="insertMember">
INSERT INTO tbl_member(
	user_id, 
	user_pw, 
	user_name, 
	email, 
	point, 
	enabled, 
	levels, 
	reg_date, 
	) 
	VALUES (
	#{user_id}, 
	#{user_pw}, 
	#{user_name}, 
	#{email}, 
	#{point}, 
	#{enabled}, 
	#{levels}, 
	now(), 
)
</insert>
</mapper>